# TODO - Correções sem alterar lógica

- [ ] Entender e localizar no `app.js` o bloco ENEM que está duplicando/confitando com `modosimulados/modos_js/enem.js`.
- [x] Corrigir a URL quebrada dentro do bloco ENEM do `app.js` (template string).

- [x] Ajustar o `app.js` para **não interferir** no modo ENEM que roda em `modosimulados/enem.html` (garantir que listeners/DOM não rodem onde não existem).

- [x] Rodar/validar: abrir `simulado.html` e confirmar que carregar JSON manual ainda funciona.
- [x] Rodar/validar: abrir `modosimulados/enem.html`, buscar questões, e confirmar que injeta corretamente no `inputSimuladoJson` do motor.



